{
  "name": "nyquest-claude-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "Nyquest Context Manager for Claude Code: MCP server + PostToolUse hook",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test \"test/**/*.test.js\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "allowScripts": {
    "esbuild@0.24.2": true
  }
}
