---
name: savings
description: Show what Nyquest has kept out of the context this session. Use when the user runs /nyquest:savings or asks how many tokens Nyquest saved.
user-invocable: true
---

Call the Nyquest `savings` tool and relay its output. Keep the "estimated" label: the
real per-session numbers come from `/cost`, and the plugin's figures use a fixed
characters-per-token ratio.
